import {connect,} from 'react-redux';
import {Route,} from '../Components';

const mapProps = (state, {path, render,}) => {
    return {

    };
};
const mapDispatch = () => ({});
export default connect(mapProps, mapDispatch)(Route);
